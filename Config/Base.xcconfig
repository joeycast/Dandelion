// Default values (used if Secrets.xcconfig doesn't exist)
TEAM_ID =
APP_BUNDLE_ID = com.example.Dandelion
TESTS_BUNDLE_ID = com.example.DandelionTests
ICLOUD_CONTAINER_ID = iCloud.com.example.Dandelion

// Override with actual values from Secrets (not committed to git)
#include "Secrets.xcconfig"
